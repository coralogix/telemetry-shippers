{
  "$defs": {
    "collector_config_confignet_tcp_addr_config": {
      "$defs": {
        "addr_config": {
          "description": "AddrConfig represents a network endpoint address.",
          "properties": {
            "dialer": {
              "$ref": "#/$defs/collector_config_confignet_tcp_addr_config/$defs/dialer_config"
            },
            "endpoint": {
              "description": "Endpoint configures the address for this network connection. For TCP and UDP networks, the address has the form \"host:port\". The host must be a literal IP address, or a host name that can be resolved to IP addresses. The port must be a literal port number or a service name. If the host is a literal IPv6 address it must be enclosed in square brackets, as in \"[2001:db8::1]:80\" or \"[fe80::1%zone]:80\". The zone specifies the scope of the literal IPv6 address as defined in RFC 4007.",
              "type": "string"
            },
            "transport": {
              "$ref": "#/$defs/collector_config_confignet_tcp_addr_config/$defs/transport_type"
            }
          },
          "type": "object"
        },
        "dialer_config": {
          "description": "DialerConfig contains options for connecting to an address.",
          "properties": {
            "timeout": {
              "anyOf": [
                {
                  "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ],
              "description": "Timeout is the maximum amount of time a dial will wait for a connect to complete. The default is no timeout.",
              "x-customType": "time.Duration"
            }
          },
          "type": "object"
        },
        "transport_type": {
          "description": "TransportType represents a type of network transport protocol",
          "type": "string"
        }
      },
      "description": "TCPAddrConfig represents a TCP endpoint address.",
      "properties": {
        "dialer": {
          "$ref": "#/$defs/collector_config_confignet_tcp_addr_config/$defs/dialer_config"
        },
        "endpoint": {
          "description": "Endpoint configures the address for this network connection. The address has the form \"host:port\". The host must be a literal IP address, or a host name that can be resolved to IP addresses. The port must be a literal port number or a service name. If the host is a literal IPv6 address it must be enclosed in square brackets, as in \"[2001:db8::1]:80\" or \"[fe80::1%zone]:80\". The zone specifies the scope of the literal IPv6 address as defined in RFC 4007.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "collector_contrib_internal_k8sconfig_api_config": {
      "$defs": {
        "auth_type": {
          "description": "AuthType describes the type of authentication to use for the K8s API",
          "type": "string"
        }
      },
      "description": "APIConfig contains options relevant to connecting to the K8s API",
      "properties": {
        "auth_type": {
          "$ref": "#/$defs/collector_contrib_internal_k8sconfig_api_config/$defs/auth_type"
        },
        "context": {
          "description": "When using auth_type `kubeConfig`, override the current context.",
          "type": "string"
        },
        "kube_api_burst": {
          "description": "KubeAPIBurst is the maximum burst of requests to the Kubernetes API. Uses client-go's default (10) if unset. Increase if you see client-side throttling warnings.",
          "type": "integer"
        },
        "kube_api_qps": {
          "description": "KubeAPIQPS is the maximum number of queries per second to the Kubernetes API. Uses client-go's default (5) if unset. Increase if you see client-side throttling warnings.",
          "type": "number",
          "x-customType": "float32"
        }
      },
      "type": "object"
    },
    "collector_contrib_pkg_ottl_error_mode": {
      "$defs": {
        "arguments": {
          "description": "Arguments holds the arguments for an OTTL function, with arguments specified as fields on a struct. Argument ordering is defined",
          "x-customType": "any"
        },
        "enum": {
          "description": "Enum is how OTTL represents an enum's numeric value.",
          "type": "integer",
          "x-customType": "int64"
        },
        "enum_symbol": {
          "description": "EnumSymbol is how OTTL represents an enum's string value.",
          "type": "string"
        },
        "logic_operation": {
          "description": "LogicOperation represents the logical operations OTTL understands.",
          "type": "string"
        },
        "type_error": {
          "description": "TypeError represents that a value was not an expected type.",
          "type": "string"
        }
      },
      "description": "ErrorMode is the way OTTL should handle errors.",
      "type": "string"
    },
    "go_opentelemetry_io_collector_exporter_debugexporter": {
      "additionalProperties": false,
      "description": "Config defines configuration for debug exporter.",
      "properties": {
        "output_paths": {
          "description": "OutputPaths is a list of file paths to write logging output to. This option can only be used when use_internal_logger is false. Special strings \"stdout\" and \"stderr\" are interpreted as os.Stdout and os.Stderr respectively. All other values are treated as file paths. If not set, defaults to [\"stdout\"].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "sampling_initial": {
          "description": "SamplingInitial defines how many samples are initially logged during each second.",
          "type": "integer"
        },
        "sampling_thereafter": {
          "description": "SamplingThereafter defines the sampling rate after the initial samples are logged.",
          "type": "integer"
        },
        "sending_queue": {
          "x-customType": "go.opentelemetry.io/collector/exporter/exporterhelper.queue_batch_config",
          "x-optional": true
        },
        "use_internal_logger": {
          "description": "UseInternalLogger defines whether the exporter sends the output to the collector's internal logger.",
          "type": "boolean"
        },
        "verbosity": {
          "description": "Verbosity defines the debug exporter verbosity.",
          "type": "string",
          "x-customType": "go.opentelemetry.io/collector/config/configtelemetry.Level"
        }
      },
      "type": "object"
    },
    "go_opentelemetry_io_collector_exporter_nopexporter": {
      "additionalProperties": false,
      "type": "object"
    },
    "go_opentelemetry_io_collector_exporter_otlpexporter": {
      "allOf": [
        {
          "x-customType": "go.opentelemetry.io/collector/exporter/exporterhelper.timeout_config"
        },
        {
          "x-customType": "go.opentelemetry.io/collector/config/configgrpc.client_config"
        }
      ],
      "description": "Config defines configuration for OTLP exporter.",
      "properties": {
        "retry_on_failure": {
          "x-customType": "go.opentelemetry.io/collector/config/configretry.back_off_config"
        },
        "sending_queue": {
          "x-customType": "go.opentelemetry.io/collector/exporter/exporterhelper.queue_batch_config",
          "x-optional": true
        }
      },
      "type": "object"
    },
    "go_opentelemetry_io_collector_exporter_otlphttpexporter": {
      "$defs": {
        "encoding_type": {
          "description": "EncodingType defines the type for content encoding",
          "type": "string"
        }
      },
      "allOf": [
        {
          "x-customType": "go.opentelemetry.io/collector/config/confighttp.client_config"
        }
      ],
      "description": "Config defines configuration for OTLP/HTTP exporter.",
      "properties": {
        "encoding": {
          "$ref": "#/$defs/go_opentelemetry_io_collector_exporter_otlphttpexporter/$defs/encoding_type"
        },
        "logs_endpoint": {
          "description": "The URL to send logs to. If omitted the Endpoint + \"/v1/logs\" will be used.",
          "type": "string"
        },
        "metrics_endpoint": {
          "description": "The URL to send metrics to. If omitted the Endpoint + \"/v1/metrics\" will be used.",
          "type": "string"
        },
        "profiles_endpoint": {
          "description": "The URL to send profiles to. If omitted the Endpoint + \"/v1development/profiles\" will be used.",
          "type": "string"
        },
        "retry_on_failure": {
          "x-customType": "go.opentelemetry.io/collector/config/configretry.back_off_config"
        },
        "sending_queue": {
          "x-customType": "go.opentelemetry.io/collector/exporter/exporterhelper.queue_batch_config",
          "x-optional": true
        },
        "traces_endpoint": {
          "description": "The URL to send traces to. If omitted the Endpoint + \"/v1/traces\" will be used.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "go_opentelemetry_io_collector_processor_batchprocessor": {
      "additionalProperties": false,
      "description": "Config defines configuration for batch processor.",
      "properties": {
        "metadata_cardinality_limit": {
          "description": "MetadataCardinalityLimit indicates the maximum number of batcher instances that will be created through a distinct combination of MetadataKeys.",
          "type": "integer",
          "x-customType": "uint32"
        },
        "metadata_keys": {
          "description": "MetadataKeys is a list of client.Metadata keys that will be used to form distinct batchers.  If this setting is empty, a single batcher instance will be used.  When this setting is not empty, one batcher will be used per distinct combination of values for the listed metadata keys. Empty value and unset metadata are treated as distinct cases. Entries are case-insensitive.  Duplicated entries will trigger a validation error.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "send_batch_max_size": {
          "description": "SendBatchMaxSize is the maximum size of a batch. It must be larger than SendBatchSize. Larger batches are split into smaller units. Default value is 0, that means no maximum size.",
          "type": "integer",
          "x-customType": "uint32"
        },
        "send_batch_size": {
          "description": "SendBatchSize is the size of a batch which after hit, will trigger it to be sent. When this is set to zero, the batch size is ignored and data will be sent immediately subject to only send_batch_max_size.",
          "type": "integer",
          "x-customType": "uint32"
        },
        "timeout": {
          "anyOf": [
            {
              "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            },
            {
              "type": "integer"
            }
          ],
          "description": "Timeout sets the time after which a batch will be sent regardless of size. When this is set to zero, batched data will be sent immediately.",
          "x-customType": "time.Duration"
        }
      },
      "type": "object"
    },
    "go_opentelemetry_io_collector_processor_memorylimiterprocessor": {},
    "go_opentelemetry_io_collector_receiver_nopreceiver": {
      "additionalProperties": false,
      "type": "object"
    },
    "internal_akamai_config": {
      "properties": {
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_alibaba_ecs_config": {
      "description": "Config defines user-specified configurations unique to the Alibaba Cloud ECS detector.",
      "properties": {
        "fail_on_missing_metadata": {
          "description": "FailOnMissingMetadata, if true, causes the detector to return an error when the Alibaba Cloud ECS metadata service is unavailable or required fields are missing. If false (default), the detector does best-effort population.",
          "type": "boolean"
        },
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_aws_ec2_config": {
      "description": "Config defines user-specified configurations unique to the EC2 detector",
      "properties": {
        "fail_on_missing_metadata": {
          "type": "boolean"
        },
        "max_attempts": {
          "type": "integer"
        },
        "max_backoff": {
          "anyOf": [
            {
              "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            },
            {
              "type": "integer"
            }
          ]
        },
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        },
        "tags": {
          "description": "Tags is a list of regex's to match ec2 instance tag keys that users want to add as resource attributes to processed data",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "tags_from_imds": {
          "description": "TagsFromIMDS controls whether instance tags are fetched via IMDS (true) or via the EC2 DescribeTags API (false, default). IMDS does not require IAM permissions but requires InstanceMetadataTags=enabled on the instance. Note: IMDS must be network-accessible from the process; workloads running in containers (e.g., EKS pods, ECS tasks) may not have access to IMDS even if it is enabled on the host, unless the instance metadata options are configured to allow container access.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "internal_aws_ecs_config": {
      "properties": {
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_aws_eks_config": {
      "properties": {
        "node_from_env_var": {
          "type": "string"
        },
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_aws_elasticbeanstalk_config": {
      "properties": {
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_aws_lambda_config": {
      "properties": {
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_azure_aks_config": {
      "properties": {
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_azure_config": {
      "properties": {
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        },
        "tags": {
          "description": "Tags is a list of regex's to match azure instance tag keys that users want to add as resource attributes to processed data",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "internal_common_context_statements": {
      "$defs": {
        "context_id": {
          "type": "string"
        }
      },
      "properties": {
        "conditions": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "context": {
          "$ref": "#/$defs/internal_common_context_statements/$defs/context_id"
        },
        "error_mode": {
          "$ref": "#/$defs/collector_contrib_pkg_ottl_error_mode"
        },
        "statements": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "internal_consul_config": {
      "description": "The struct requires no user-specified fields by default as consul agent's default configuration will be provided to the API client. See `consul.go#NewDetector` for more information.",
      "properties": {
        "address": {
          "description": "Address is the address of the Consul server",
          "type": "string"
        },
        "datacenter": {
          "description": "Datacenter to use. If not provided, the default agent datacenter is used.",
          "type": "string"
        },
        "meta": {
          "additionalProperties": {
            "x-customType": "any"
          },
          "description": "Allowlist of [Consul Metadata](https://www.consul.io/docs/agent/options#node_meta) keys to use as resource attributes.",
          "type": "object"
        },
        "namespace": {
          "description": "Namespace is the name of the namespace to send along for the request when no other Namespace is present in the QueryOptions",
          "type": "string"
        },
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        },
        "token": {
          "$ref": "#/$defs/opentelemetry_io_collector_config_configopaque_string"
        },
        "token_file": {
          "description": "TokenFile is a file containing the current token to use for this client. If provided it is read once at startup and never again. Token or Tokenfile are only required if [Consul's ACL System](https://www.consul.io/docs/security/acl/acl-system) is enabled.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "internal_coreinternal_attraction_action_key_value": {
      "$defs": {
        "action": {
          "description": "Action is the enum to capture the four types of actions to perform on an attribute.",
          "type": "string"
        },
        "action_key_value": {
          "type": "object",
          "x-missingRef": "#/$defs/internal_coreinternal_attraction_action_key_value/$defs/action_key_value"
        },
        "settings": {
          "description": "Settings specifies the processor settings.",
          "properties": {
            "actions": {
              "description": "Actions specifies the list of attributes to act on. The set of actions are {INSERT, UPDATE, UPSERT, DELETE, HASH, EXTRACT, CONVERT}. This is a required field.",
              "items": {
                "$ref": "#/$defs/internal_coreinternal_attraction_action_key_value/$defs/action_key_value"
              },
              "type": "array"
            }
          },
          "type": "object"
        }
      },
      "description": "ActionKeyValue specifies the attribute key to act upon.",
      "properties": {
        "action": {
          "$ref": "#/$defs/internal_coreinternal_attraction_action_key_value/$defs/action"
        },
        "converted_type": {
          "description": "ConvertedType specifies the target type of an attribute to be converted If the key doesn't exist, no action is performed. If the value cannot be converted, the original value will be left as-is",
          "type": "string"
        },
        "default_value": {
          "description": "DefaultValue specifies the value to use if Value/FromAttribute/FromContext doesn't provide a value (e.g., environment variable not set, attribute doesn't exist). Only used with INSERT, UPDATE, and UPSERT actions.",
          "x-customType": "any"
        },
        "from_attribute": {
          "description": "FromAttribute specifies the attribute to use to populate the value. If the attribute doesn't exist, no action is performed.",
          "type": "string"
        },
        "from_context": {
          "description": "FromContext specifies the context value to use to populate the value. The values would be searched in client.Info.Metadata. If the key doesn't exist, no action is performed. If the key has multiple values the values will be joined with `;` separator.",
          "type": "string"
        },
        "key": {
          "description": "Key specifies the attribute to act upon. This is a required field.",
          "type": "string"
        },
        "pattern": {
          "description": "A regex pattern must be specified for the action EXTRACT. It uses the attribute specified by `key' to extract values from The target keys are inferred based on the names of the matcher groups provided and the names will be inferred based on the values of the matcher group. Note: All subexpressions must have a name. Note: The value type of the source key must be a string. If it isn't, no extraction will occur.",
          "type": "string"
        },
        "value": {
          "description": "Value specifies the value to populate for the key. The type of the value is inferred from the configuration.",
          "x-customType": "any"
        }
      },
      "type": "object"
    },
    "internal_digitalocean_config": {
      "properties": {
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_docker_config": {
      "properties": {
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_gcp_config": {
      "properties": {
        "labels": {
          "description": "Labels is a list of regex's to match gce instance label keys that users want to add as resource attributes to processed data",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_heroku_config": {
      "properties": {
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_hetzner_config": {
      "properties": {
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_ibmcloud_classic_config": {
      "additionalProperties": false,
      "description": "Config defines user-specified configurations unique to the IBM Cloud Classic detector.",
      "properties": {
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_ibmcloud_vpc_config": {
      "additionalProperties": false,
      "description": "Config defines user-specified configurations unique to the IBM Cloud VPC detector.",
      "properties": {
        "protocol": {
          "description": "Protocol selects the scheme used to reach the IMDS endpoint. Accepted values are \"http\" (default) and \"https\". Both use the host api.metadata.cloud.ibm.com.",
          "type": "string"
        },
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_k8sapi_config": {
      "allOf": [
        {
          "$ref": "#/$defs/collector_contrib_internal_k8sconfig_api_config"
        }
      ],
      "properties": {
        "node_from_env_var": {
          "description": "NodeFromEnv can be used to extract the node name from an environment variable. The value must be the name of the environment variable. This is useful when the node a Otel agent will run on cannot be predicted. In such cases, the Kubernetes downward API can be used to add the node name to each pod as an environment variable. K8s tagger can then read this value and filter pods by it. NodeFromEnv defaults to K8S_NODE_NAME For example, node name can be passed to each agent with the downward API as follows env: - name: K8S_NODE_NAME valueFrom: fieldRef: fieldPath: spec.nodeName Then the NodeFromEnv field can be set to `K8S_NODE_NAME` to filter all pods by the node that the agent is running on. More on downward API here: https://kubernetes.io/docs/tasks/inject-data-application/environment-variable-expose-pod-information/",
          "type": "string"
        },
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_kubeadm_config": {
      "allOf": [
        {
          "$ref": "#/$defs/collector_contrib_internal_k8sconfig_api_config"
        }
      ],
      "properties": {
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_metadata_resource_attributes_config": {
      "$defs": {},
      "description": "ResourceAttributesConfig provides config for resourcedetectionprocessor/k8s_api resource attributes.",
      "properties": {
        "k8s.cluster.uid": {
          "description": "ResourceAttributeConfig provides common config for a k8s.cluster.uid resource attribute.",
          "properties": {
            "enabled": {
              "default": true,
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "k8s.node.name": {
          "description": "ResourceAttributeConfig provides common config for a k8s.node.name resource attribute.",
          "properties": {
            "enabled": {
              "default": true,
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "k8s.node.uid": {
          "description": "ResourceAttributeConfig provides common config for a k8s.node.uid resource attribute.",
          "properties": {
            "enabled": {
              "default": true,
              "type": "boolean"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "internal_openshift_config": {
      "description": "Config can contain user-specified inputs to overwrite default values. See `openshift.go#NewDetector` for more information.",
      "properties": {
        "address": {
          "description": "Address is the address of the openshift api server",
          "type": "string"
        },
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        },
        "tls": {
          "$ref": "#/$defs/io_collector_config_configtls_client_config"
        },
        "token": {
          "description": "Token is used to identify against the openshift api server",
          "type": "string"
        }
      },
      "type": "object"
    },
    "internal_openstack_nova_config": {
      "description": "Config defines user-specified configurations unique to the Nova detector.",
      "properties": {
        "fail_on_missing_metadata": {
          "description": "FailOnMissingMetadata, if true, causes the detector to return an error when the Nova metadata service is unavailable or required fields are missing. If false (default), the detector does best-effort population.",
          "type": "boolean"
        },
        "labels": {
          "description": "Labels is a list of regex patterns to match Nova instance metadata keys (from the \"meta\" map) that should be added as resource attributes. Matched keys are emitted as \"openstack.nova.meta.\u003ckey\u003e: \u003cvalue\u003e\".",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_oraclecloud_config": {
      "properties": {
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_scaleway_config": {
      "properties": {
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_system_config": {
      "description": "Config defines user-specified configurations unique to the system detector",
      "properties": {
        "hostname_sources": {
          "description": "The HostnameSources is a priority list of sources from which hostname will be fetched. In case of the error in fetching hostname from source, the next source from the list will be considered.(**default**: `[\"dns\", \"os\"]`)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_tencent_cvm_config": {
      "additionalProperties": false,
      "description": "Config defines user-specified configurations unique to the Tencent Cloud CVM detector.",
      "properties": {
        "fail_on_missing_metadata": {
          "description": "FailOnMissingMetadata, if true, causes the detector to return an error when the Tencent Cloud CVM metadata service is unavailable or any required field is missing. If false (default), the detector logs the failure and returns an empty resource (no attributes set).",
          "type": "boolean"
        },
        "resource_attributes": {
          "description": "ResourceAttributes controls which standard resource attributes are enabled (e.g., host.id, host.name, cloud.provider, etc.).",
          "x-customType": "./internal/metadata.resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_upcloud_config": {
      "properties": {
        "fail_on_missing_metadata": {
          "type": "boolean"
        },
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "internal_vultr_config": {
      "properties": {
        "fail_on_missing_metadata": {
          "type": "boolean"
        },
        "resource_attributes": {
          "$ref": "#/$defs/internal_metadata_resource_attributes_config"
        }
      },
      "type": "object"
    },
    "io_collector_config_configcompression_compression_params": {
      "$defs": {
        "level": {
          "type": "integer"
        },
        "type": {
          "description": "Type represents a compression method",
          "type": "string"
        }
      },
      "properties": {
        "level": {
          "$ref": "#/$defs/io_collector_config_configcompression_compression_params/$defs/level"
        }
      },
      "type": "object"
    },
    "io_collector_config_confighttp_client_config": {
      "$defs": {
        "auth_config": {
          "allOf": [
            {
              "$ref": "#/$defs/opentelemetry_io_collector_config_configauth_config"
            }
          ],
          "properties": {
            "request_params": {
              "description": "RequestParameters is a list of parameters that should be extracted from the request and added to the context. When a parameter is found in both the query string and the header, the value from the query string will be used.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "type": "object"
        },
        "cookies_config": {
          "description": "CookiesConfig defines the configuration of the HTTP client regarding cookies served by the server."
        },
        "cors_config": {
          "description": "CORSConfig configures a receiver for HTTP cross-origin resource sharing (CORS). See the underlying https://github.com/rs/cors package for details.",
          "properties": {
            "allowed_headers": {
              "description": "AllowedHeaders sets what headers will be allowed in CORS requests. The Accept, Accept-Language, Content-Type, and Content-Language headers are implicitly allowed. If no headers are listed, X-Requested-With will also be accepted by default. Include \"*\" to allow any request header.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "allowed_origins": {
              "description": "AllowedOrigins sets the allowed values of the Origin header for HTTP/JSON requests to an OTLP receiver. An origin may contain a wildcard (*) to replace 0 or more characters (e.g., \"http://*.domain.com\", or \"*\" to allow any origin).",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "exposed_headers": {
              "description": "ExposedHeaders sets the value of the Access-Control-Expose-Headers response header, indicating which headers are safe to expose to the API of a CORS response.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "max_age": {
              "description": "MaxAge sets the value of the Access-Control-Max-Age response header. Set it to the number of seconds that browsers should cache a CORS preflight response for.",
              "type": "integer"
            }
          },
          "type": "object"
        },
        "server_config": {
          "description": "ServerConfig defines settings for creating an HTTP server.",
          "properties": {
            "auth": {
              "$ref": "#/$defs/io_collector_config_confighttp_client_config/$defs/auth_config"
            },
            "compression_algorithms": {
              "description": "CompressionAlgorithms configures the list of compression algorithms the server can accept. Default: [\"\", \"gzip\", \"zstd\", \"zlib\", \"snappy\", \"deflate\"]",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "cors": {
              "$ref": "#/$defs/io_collector_config_confighttp_client_config/$defs/cors_config"
            },
            "endpoint": {
              "description": "Endpoint configures the listening address for the server.",
              "type": "string"
            },
            "idle_timeout": {
              "anyOf": [
                {
                  "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ],
              "description": "IdleTimeout is the maximum amount of time to wait for the next request when keep-alives are enabled. If IdleTimeout is zero, the value of ReadTimeout is used. If both are zero, there is no timeout.",
              "x-customType": "time.Duration"
            },
            "include_metadata": {
              "description": "IncludeMetadata propagates the client metadata from the incoming requests to the downstream consumers",
              "type": "boolean"
            },
            "keep_alives_enabled": {
              "description": "KeepAlivesEnabled controls whether HTTP keep-alives are enabled. By default, keep-alives are always enabled. Only very resource-constrained environments should disable them.",
              "type": "boolean"
            },
            "max_request_body_size": {
              "description": "MaxRequestBodySize sets the maximum request body size in bytes. Default: 20MiB.",
              "type": "integer",
              "x-customType": "int64"
            },
            "middlewares": {
              "description": "Middlewares are used to add custom functionality to the HTTP server. Middleware handlers are called in the order they appear in this list, with the first middleware becoming the outermost handler.",
              "items": {
                "$ref": "#/$defs/opentelemetry_io_collector_config_configmiddleware_config"
              },
              "type": "array"
            },
            "read_header_timeout": {
              "anyOf": [
                {
                  "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ],
              "description": "ReadHeaderTimeout is the amount of time allowed to read request headers. The connection's read deadline is reset after reading the headers and the Handler can decide what is considered too slow for the body. If ReadHeaderTimeout is zero, the value of ReadTimeout is used. If both are zero, there is no timeout.",
              "x-customType": "time.Duration"
            },
            "read_timeout": {
              "anyOf": [
                {
                  "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ],
              "description": "ReadTimeout is the maximum duration for reading the entire request, including the body. A zero or negative value means there will be no timeout. Because ReadTimeout does not let Handlers make per-request decisions on each request body's acceptable deadline or upload rate, most users will prefer to use ReadHeaderTimeout. It is valid to use them both.",
              "x-customType": "time.Duration"
            },
            "response_headers": {
              "$ref": "#/$defs/io_collector_config_configopaque_map_list"
            },
            "tls": {
              "$ref": "#/$defs/io_collector_config_configtls_server_config"
            },
            "write_timeout": {
              "anyOf": [
                {
                  "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ],
              "description": "WriteTimeout is the maximum duration before timing out writes of the response. It is reset whenever a new request's header is read. Like ReadTimeout, it does not let Handlers make decisions on a per-request basis. A zero or negative value means there will be no timeout.",
              "x-customType": "time.Duration"
            }
          },
          "type": "object"
        }
      },
      "description": "ClientConfig defines settings for creating an HTTP client.",
      "properties": {
        "auth": {
          "$ref": "#/$defs/opentelemetry_io_collector_config_configauth_config"
        },
        "compression": {
          "$ref": "#/$defs/opentelemetry_io_collector_config_configcompression_type"
        },
        "compression_params": {
          "$ref": "#/$defs/io_collector_config_configcompression_compression_params"
        },
        "cookies": {
          "$ref": "#/$defs/io_collector_config_confighttp_client_config/$defs/cookies_config"
        },
        "disable_keep_alives": {
          "description": "DisableKeepAlives, if true, disables HTTP keep-alives and will only use the connection to the server for a single HTTP request. WARNING: enabling this option can result in significant overhead establishing a new HTTP(S) connection for every request. Before enabling this option please consider whether changes to idle connection settings can achieve your goal.",
          "type": "boolean"
        },
        "endpoint": {
          "description": "The target URL to send data to (e.g.: http://some.url:9411/v1/traces).",
          "type": "string"
        },
        "force_attempt_http2": {
          "description": "Enabling ForceAttemptHTTP2 forces the HTTP transport to use the HTTP/2 protocol. By default, this is set to true. NOTE: HTTP/2 does not support settings such as MaxConnsPerHost, MaxIdleConnsPerHost and MaxIdleConns.",
          "type": "boolean"
        },
        "headers": {
          "$ref": "#/$defs/io_collector_config_configopaque_map_list"
        },
        "http2_ping_timeout": {
          "anyOf": [
            {
              "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            },
            {
              "type": "integer"
            }
          ],
          "description": "HTTP2PingTimeout if there's no response to the ping within the configured value, the connection will be closed. If not set or set to 0, it defaults to 15s.",
          "x-customType": "time.Duration"
        },
        "http2_read_idle_timeout": {
          "anyOf": [
            {
              "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            },
            {
              "type": "integer"
            }
          ],
          "description": "This is needed in case you run into https://github.com/golang/go/issues/59690 https://github.com/golang/go/issues/36026 HTTP2ReadIdleTimeout if the connection has been idle for the configured value send a ping frame for health check 0s means no health check will be performed.",
          "x-customType": "time.Duration"
        },
        "idle_conn_timeout": {
          "anyOf": [
            {
              "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            },
            {
              "type": "integer"
            }
          ],
          "description": "IdleConnTimeout is the maximum amount of time a connection will remain open before closing itself. By default, it is set to 90 seconds.",
          "x-customType": "time.Duration"
        },
        "max_conns_per_host": {
          "description": "MaxConnsPerHost limits the total number of connections per host, including connections in the dialing, active, and idle states. Default is 0 (unlimited).",
          "type": "integer"
        },
        "max_idle_conns": {
          "description": "MaxIdleConns is used to set a limit to the maximum idle HTTP connections the client can keep open. By default, it is set to 100. Zero means no limit.",
          "type": "integer"
        },
        "max_idle_conns_per_host": {
          "description": "MaxIdleConnsPerHost is used to set a limit to the maximum idle HTTP connections the host can keep open. If zero, [net/http.DefaultMaxIdleConnsPerHost] is used.",
          "type": "integer"
        },
        "middlewares": {
          "description": "Middlewares are used to add custom functionality to the HTTP client. Middleware handlers are called in the order they appear in this list, with the first middleware becoming the outermost handler.",
          "items": {
            "$ref": "#/$defs/opentelemetry_io_collector_config_configmiddleware_config"
          },
          "type": "array"
        },
        "proxy_url": {
          "description": "ProxyURL setting for the collector",
          "type": "string"
        },
        "read_buffer_size": {
          "description": "ReadBufferSize for HTTP client. See http.Transport.ReadBufferSize. Default is 0.",
          "type": "integer"
        },
        "timeout": {
          "anyOf": [
            {
              "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            },
            {
              "type": "integer"
            }
          ],
          "description": "Timeout parameter configures `http.Client.Timeout`. Default is 0 (unlimited).",
          "x-customType": "time.Duration"
        },
        "tls": {
          "$ref": "#/$defs/io_collector_config_configtls_client_config"
        },
        "write_buffer_size": {
          "description": "WriteBufferSize for HTTP client. See http.Transport.WriteBufferSize. Default is 0.",
          "type": "integer"
        }
      },
      "type": "object"
    },
    "io_collector_config_configopaque_map_list": {
      "$defs": {
        "pair": {
          "description": "Pair is an element of a MapList, and consists of a name and an opaque value.",
          "properties": {
            "name": {
              "type": "string"
            },
            "value": {
              "$ref": "#/$defs/io_collector_config_configopaque_map_list/$defs/string"
            }
          },
          "type": "object"
        },
        "string": {
          "description": "String alias that is marshaled and printed in an opaque way. To recover the original value, cast it to a string.",
          "type": "string"
        }
      },
      "anyOf": [
        {
          "items": {
            "$ref": "#/$defs/io_collector_config_configopaque_map_list/$defs/pair"
          },
          "type": "array"
        },
        {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object"
        }
      ],
      "description": "MapList is a replacement for map[string]configopaque.String with a similar API, which can also be unmarshalled from (and is stored as) a list of name/value pairs. Pairs are assumed to have distinct names. This is checked during config validation."
    },
    "io_collector_config_configtls_client_config": {
      "$defs": {
        "config": {
          "description": "Config exposes the common client and server TLS configurations. Note: Since there isn't anything specific to a server connection. Components with server connections should use Config.",
          "properties": {
            "ca_file": {
              "description": "Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)",
              "type": "string"
            },
            "ca_pem": {
              "$ref": "#/$defs/opentelemetry_io_collector_config_configopaque_string"
            },
            "cert_file": {
              "description": "Path to the TLS cert to use for TLS required connections. (optional)",
              "type": "string"
            },
            "cert_pem": {
              "$ref": "#/$defs/opentelemetry_io_collector_config_configopaque_string"
            },
            "cipher_suites": {
              "description": "CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "curve_preferences": {
              "description": "contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and \"crypto/tls\" defaults are used, internally.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "include_system_ca_certs_pool": {
              "description": "If true, load system CA certificates pool in addition to the certificates configured in this struct.",
              "type": "boolean"
            },
            "key_file": {
              "description": "Path to the TLS key to use for TLS required connections. (optional)",
              "type": "string"
            },
            "key_pem": {
              "$ref": "#/$defs/opentelemetry_io_collector_config_configopaque_string"
            },
            "max_version": {
              "description": "MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)",
              "type": "string"
            },
            "min_version": {
              "description": "MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)",
              "type": "string"
            },
            "reload_interval": {
              "anyOf": [
                {
                  "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ],
              "description": "ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)",
              "x-customType": "time.Duration"
            },
            "tpm": {
              "$ref": "#/$defs/io_collector_config_configtls_client_config/$defs/tpm_config"
            }
          },
          "type": "object"
        },
        "server_config": {
          "allOf": [
            {
              "$ref": "#/$defs/io_collector_config_configtls_client_config/$defs/config"
            }
          ],
          "description": "ServerConfig contains TLS configurations that are specific to server connections in addition to the common configurations. This should be used by components configuring TLS server connections.",
          "properties": {
            "client_ca_file": {
              "description": "Path to the TLS cert to use by the server to verify a client certificate. (optional) This sets the ClientCAs and ClientAuth to RequireAndVerifyClientCert in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)",
              "type": "string"
            },
            "client_ca_file_reload": {
              "description": "Reload the ClientCAs file when it is modified (optional, default false)",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "tpm_config": {
          "description": "TPMConfig defines trusted platform module configuration for storing TLS keys.",
          "properties": {
            "auth": {
              "type": "string"
            },
            "enabled": {
              "type": "boolean"
            },
            "owner_auth": {
              "type": "string"
            },
            "path": {
              "description": "The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.",
              "type": "string"
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/io_collector_config_configtls_client_config/$defs/config"
        }
      ],
      "description": "ClientConfig contains TLS configurations that are specific to client connections in addition to the common configurations. This should be used by components configuring TLS client connections.",
      "properties": {
        "insecure": {
          "description": "In gRPC and HTTP when set to true, this is used to disable the client transport security. See https://godoc.org/google.golang.org/grpc#WithInsecure for gRPC. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional, default false)",
          "type": "boolean"
        },
        "insecure_skip_verify": {
          "description": "InsecureSkipVerify will enable TLS but not verify the certificate.",
          "type": "boolean"
        },
        "server_name_override": {
          "description": "ServerName requested by client for virtual hosting. This sets the ServerName in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)",
          "type": "string"
        }
      },
      "type": "object"
    },
    "io_collector_config_configtls_server_config": {
      "$defs": {
        "client_config": {
          "allOf": [
            {
              "$ref": "#/$defs/io_collector_config_configtls_server_config/$defs/config"
            }
          ],
          "description": "ClientConfig contains TLS configurations that are specific to client connections in addition to the common configurations. This should be used by components configuring TLS client connections.",
          "properties": {
            "insecure": {
              "description": "In gRPC and HTTP when set to true, this is used to disable the client transport security. See https://godoc.org/google.golang.org/grpc#WithInsecure for gRPC. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional, default false)",
              "type": "boolean"
            },
            "insecure_skip_verify": {
              "description": "InsecureSkipVerify will enable TLS but not verify the certificate.",
              "type": "boolean"
            },
            "server_name_override": {
              "description": "ServerName requested by client for virtual hosting. This sets the ServerName in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)",
              "type": "string"
            }
          },
          "type": "object"
        },
        "config": {
          "description": "Config exposes the common client and server TLS configurations. Note: Since there isn't anything specific to a server connection. Components with server connections should use Config.",
          "properties": {
            "ca_file": {
              "description": "Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)",
              "type": "string"
            },
            "ca_pem": {
              "$ref": "#/$defs/opentelemetry_io_collector_config_configopaque_string"
            },
            "cert_file": {
              "description": "Path to the TLS cert to use for TLS required connections. (optional)",
              "type": "string"
            },
            "cert_pem": {
              "$ref": "#/$defs/opentelemetry_io_collector_config_configopaque_string"
            },
            "cipher_suites": {
              "description": "CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "curve_preferences": {
              "description": "contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and \"crypto/tls\" defaults are used, internally.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "include_system_ca_certs_pool": {
              "description": "If true, load system CA certificates pool in addition to the certificates configured in this struct.",
              "type": "boolean"
            },
            "key_file": {
              "description": "Path to the TLS key to use for TLS required connections. (optional)",
              "type": "string"
            },
            "key_pem": {
              "$ref": "#/$defs/opentelemetry_io_collector_config_configopaque_string"
            },
            "max_version": {
              "description": "MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)",
              "type": "string"
            },
            "min_version": {
              "description": "MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)",
              "type": "string"
            },
            "reload_interval": {
              "anyOf": [
                {
                  "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ],
              "description": "ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)",
              "x-customType": "time.Duration"
            },
            "tpm": {
              "$ref": "#/$defs/io_collector_config_configtls_server_config/$defs/tpm_config"
            }
          },
          "type": "object"
        },
        "tpm_config": {
          "description": "TPMConfig defines trusted platform module configuration for storing TLS keys.",
          "properties": {
            "auth": {
              "type": "string"
            },
            "enabled": {
              "type": "boolean"
            },
            "owner_auth": {
              "type": "string"
            },
            "path": {
              "description": "The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.",
              "type": "string"
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/io_collector_config_configtls_server_config/$defs/config"
        }
      ],
      "description": "ServerConfig contains TLS configurations that are specific to server connections in addition to the common configurations. This should be used by components configuring TLS server connections.",
      "properties": {
        "client_ca_file": {
          "description": "Path to the TLS cert to use by the server to verify a client certificate. (optional) This sets the ClientCAs and ClientAuth to RequireAndVerifyClientCert in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)",
          "type": "string"
        },
        "client_ca_file_reload": {
          "description": "Reload the ClientCAs file when it is modified (optional, default false)",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "opentelemetry_collector_contrib_internal_healthcheck_config": {
      "$defs": {
        "check_collector_pipeline_config": {
          "description": "Type aliases to expose internal types publicly",
          "x-customType": "./internal/http.check_collector_pipeline_config"
        },
        "component_health_config": {
          "description": "Type aliases to expose internal types publicly",
          "x-customType": "./internal/common.component_health_config"
        },
        "grpc_config": {
          "description": "Type aliases to expose internal types publicly",
          "x-customType": "./internal/grpc.config"
        },
        "health_check_extension": {
          "additionalProperties": false,
          "type": "object"
        },
        "http_config": {
          "description": "Type aliases to expose internal types publicly",
          "x-customType": "./internal/http.config"
        },
        "http_legacy_config": {
          "description": "Type aliases to expose internal types publicly",
          "x-customType": "./internal/http.legacy_config"
        },
        "path_config": {
          "description": "Type aliases to expose internal types publicly",
          "x-customType": "./internal/http.path_config"
        },
        "response_body_config": {
          "description": "Type aliases to expose internal types publicly",
          "x-customType": "./internal/http.response_body_config"
        }
      },
      "allOf": [
        {
          "x-customType": "./internal/http.legacy_config"
        }
      ],
      "description": "Config has the configuration for the extension enabling the health check extension, used to report the health status of the service.",
      "properties": {
        "component_health": {
          "description": "ComponentHealthConfig is v2 config shared between http and grpc services",
          "x-customType": "./internal/common.component_health_config",
          "x-pointer": true
        },
        "grpc": {
          "description": "GRPCConfig is v2 config for the grpc healthcheck service.",
          "x-customType": "./internal/grpc.config",
          "x-pointer": true
        },
        "http": {
          "description": "HTTPConfig is v2 config for the http healthcheck service.",
          "x-customType": "./internal/http.config",
          "x-pointer": true
        }
      },
      "type": "object"
    },
    "opentelemetry_io_collector_config_configauth_config": {
      "$defs": {},
      "description": "Config defines the auth settings for the receiver.",
      "properties": {
        "authenticator": {
          "description": "AuthenticatorID specifies the name of the extension to use in order to authenticate the incoming data point.",
          "type": "string",
          "x-customType": "go.opentelemetry.io/collector/component.ID"
        }
      },
      "type": "object"
    },
    "opentelemetry_io_collector_config_configcompression_type": {
      "$defs": {
        "compression_params": {
          "properties": {
            "level": {
              "$ref": "#/$defs/opentelemetry_io_collector_config_configcompression_type/$defs/level"
            }
          },
          "type": "object"
        },
        "level": {
          "type": "integer"
        }
      },
      "description": "Type represents a compression method",
      "type": "string"
    },
    "opentelemetry_io_collector_config_configmiddleware_config": {
      "$defs": {},
      "description": "Defines the extension ID for a middleware component.",
      "properties": {
        "id": {
          "description": "Specifies the name of the extension to use.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "opentelemetry_io_collector_config_configopaque_string": {
      "$defs": {
        "map_list": {
          "anyOf": [
            {
              "items": {
                "$ref": "#/$defs/opentelemetry_io_collector_config_configopaque_string/$defs/pair"
              },
              "type": "array"
            },
            {
              "additionalProperties": {
                "type": "string"
              },
              "type": "object"
            }
          ],
          "description": "MapList is a replacement for map[string]configopaque.String with a similar API, which can also be unmarshalled from (and is stored as) a list of name/value pairs. Pairs are assumed to have distinct names. This is checked during config validation."
        },
        "pair": {
          "description": "Pair is an element of a MapList, and consists of a name and an opaque value.",
          "properties": {
            "name": {
              "type": "string"
            },
            "value": {
              "$ref": "#/$defs/opentelemetry_io_collector_config_configopaque_string/$defs/string"
            }
          },
          "type": "object"
        },
        "string": {
          "type": "object",
          "x-missingRef": "#/$defs/opentelemetry_io_collector_config_configopaque_string/$defs/string"
        }
      },
      "description": "String alias that is marshaled and printed in an opaque way. To recover the original value, cast it to a string.",
      "type": "string"
    },
    "opentelemetry_io_ebpf_profiler_receiver_profilingreceiver": {},
    "telemetry_opentelemetry_collector_contrib_exporter_fileexporter": {
      "$defs": {
        "group_by": {
          "properties": {
            "enabled": {
              "description": "Enables group_by. Default is false.",
              "type": "boolean"
            },
            "max_open_files": {
              "description": "MaxOpenFiles specifies the maximum number of open file descriptors for the output files. The default is 100.",
              "type": "integer"
            },
            "resource_attribute": {
              "description": "ResourceAttribute specifies the name of the resource attribute that contains the path segment of the file to write to. The final path will be the Path config value, with the * replaced with the value of this resource attribute. Default is \"fileexporter.path_segment\".",
              "type": "string"
            }
          },
          "type": "object"
        },
        "rotation": {
          "description": "Rotation an option to rolling log files",
          "properties": {
            "localtime": {
              "description": "LocalTime determines if the time used for formatting the timestamps in backup files is the computer's local time.  The default is to use UTC time.",
              "type": "boolean"
            },
            "max_backups": {
              "description": "MaxBackups is the maximum number of old log files to retain. The default is to 100 files.",
              "type": "integer"
            },
            "max_days": {
              "description": "MaxDays is the maximum number of days to retain old log files based on the timestamp encoded in their filename.  Note that a day is defined as 24 hours and may not exactly correspond to calendar days due to daylight savings, leap seconds, etc. The default is not to remove old log files based on age.",
              "type": "integer"
            },
            "max_megabytes": {
              "description": "MaxMegabytes is the maximum size in megabytes of the file before it gets rotated. It defaults to 100 megabytes.",
              "type": "integer"
            }
          },
          "type": "object"
        }
      },
      "description": "Config defines configuration for file exporter.",
      "properties": {
        "append": {
          "description": "Mode defines whether the exporter should append to the file. Options: - false[default]:  truncates the file - true:  appends to the file.",
          "type": "boolean"
        },
        "compression": {
          "description": "Compression Codec used to export telemetry data Supported compression algorithms:`zstd`",
          "type": "string"
        },
        "compression_params": {
          "$ref": "#/$defs/io_collector_config_configcompression_compression_params"
        },
        "create_directory": {
          "description": "CreateDirectory specifies that the parent directory of the output file should be created automatically on start.",
          "type": "boolean"
        },
        "directory_permissions": {
          "description": "DirectoryPermissions specifies permissions used when creating directories (minus process umask). Value must be an octal string like \"0755\".",
          "type": "string"
        },
        "encoding": {
          "description": "Encoding defines the encoding of the telemetry data. If specified, it overrides `FormatType` and applies an encoding extension.",
          "type": "string",
          "x-customType": "go.opentelemetry.io/collector/component.ID",
          "x-pointer": true
        },
        "flush_interval": {
          "anyOf": [
            {
              "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            },
            {
              "type": "integer"
            }
          ],
          "description": "FlushInterval is the duration between flushes. See time.ParseDuration for valid values."
        },
        "format": {
          "description": "FormatType define the data format of encoded telemetry data Options: - json[default]:  OTLP json bytes. - proto:  OTLP binary protobuf bytes.",
          "type": "string"
        },
        "group_by": {
          "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_exporter_fileexporter/$defs/group_by"
        },
        "path": {
          "description": "Path of the file to write to. Path is relative to current directory.",
          "type": "string"
        },
        "rotation": {
          "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_exporter_fileexporter/$defs/rotation"
        }
      },
      "type": "object"
    },
    "telemetry_opentelemetry_collector_contrib_extension_healthcheckextension": {
      "allOf": [
        {
          "$ref": "#/$defs/opentelemetry_collector_contrib_internal_healthcheck_config"
        }
      ],
      "description": "Config wraps the shared healthcheck.Config to add extension-specific validation.",
      "type": "object"
    },
    "telemetry_opentelemetry_collector_contrib_extension_opampextension": {
      "$defs": {
        "agent_description": {
          "properties": {
            "include_resource_attributes": {
              "description": "IncludeResourceAttributes determines whether the agent should copy its resource attributes to the non identifying attributes. (default: false)",
              "type": "boolean"
            },
            "non_identifying_attributes": {
              "additionalProperties": {
                "type": "string"
              },
              "description": "NonIdentifyingAttributes are a map of key-value pairs that may be specified to provide extra information about the agent to the OpAMP server.",
              "type": "object"
            }
          },
          "type": "object"
        },
        "capabilities": {
          "properties": {
            "accepts_restart_command": {
              "description": "AcceptsRestartCommand enables the OpAMP AcceptsRestartCommand Capability (default: false)",
              "type": "boolean"
            },
            "reports_available_components": {
              "description": "ReportsAvailableComponents enables the OpAMP ReportsAvailableComponents Capability (default: true)",
              "type": "boolean"
            },
            "reports_effective_config": {
              "description": "ReportsEffectiveConfig enables the OpAMP ReportsEffectiveConfig Capability. (default: true)",
              "type": "boolean"
            },
            "reports_health": {
              "description": "ReportsHealth enables the OpAMP ReportsHealth Capability. (default: true)",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "op_amp_server": {
          "description": "OpAMPServer contains the OpAMP transport configuration.",
          "properties": {
            "http": {
              "properties": {
                "auth": {
                  "type": "string",
                  "x-customType": "go.opentelemetry.io/collector/component.ID"
                },
                "endpoint": {
                  "type": "string"
                },
                "headers": {
                  "additionalProperties": {
                    "$ref": "#/$defs/opentelemetry_io_collector_config_configopaque_string"
                  },
                  "type": "object"
                },
                "polling_interval": {
                  "anyOf": [
                    {
                      "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
                      "type": "string"
                    },
                    {
                      "type": "integer"
                    }
                  ]
                },
                "tls": {
                  "$ref": "#/$defs/io_collector_config_configtls_client_config"
                }
              },
              "type": "object",
              "x-pointer": true
            },
            "ws": {
              "properties": {
                "auth": {
                  "type": "string",
                  "x-customType": "go.opentelemetry.io/collector/component.ID"
                },
                "endpoint": {
                  "type": "string"
                },
                "headers": {
                  "additionalProperties": {
                    "$ref": "#/$defs/opentelemetry_io_collector_config_configopaque_string"
                  },
                  "type": "object"
                },
                "tls": {
                  "$ref": "#/$defs/io_collector_config_configtls_client_config"
                }
              },
              "type": "object",
              "x-pointer": true
            }
          },
          "type": "object"
        }
      },
      "description": "Config contains the configuration for the opamp extension. Trying to mirror the OpAMP supervisor config for some consistency.",
      "properties": {
        "agent_description": {
          "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_extension_opampextension/$defs/agent_description"
        },
        "capabilities": {
          "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_extension_opampextension/$defs/capabilities"
        },
        "instance_uid": {
          "description": "InstanceUID is a UUID formatted as a 36 character string in canonical representation. Auto-generated on start if missing.",
          "type": "string"
        },
        "ppid": {
          "description": "PPID is the process ID of the parent for the collector. If the PPID is specified, the extension will continuously poll for the status of the parent process, and emit a fatal error when the parent process is no longer running. If unspecified, the orphan detection logic does not run.",
          "type": "integer",
          "x-customType": "int32"
        },
        "ppid_poll_interval": {
          "anyOf": [
            {
              "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            },
            {
              "type": "integer"
            }
          ],
          "description": "PPIDPollInterval is the time between polling for whether PPID is running."
        },
        "server": {
          "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_extension_opampextension/$defs/op_amp_server"
        }
      },
      "type": "object"
    },
    "telemetry_opentelemetry_collector_contrib_extension_pprofextension": {
      "allOf": [
        {
          "$ref": "#/$defs/collector_config_confignet_tcp_addr_config"
        }
      ],
      "description": "Config has the configuration for the extension enabling the golang net/http/pprof (Performance Profiler) extension.",
      "properties": {
        "block_profile_fraction": {
          "description": "Fraction of blocking events that are profiled. A value \u003c= 0 disables profiling. See https://golang.org/pkg/runtime/#SetBlockProfileRate for details.",
          "type": "integer"
        },
        "mutex_profile_fraction": {
          "description": "Fraction of mutex contention events that are profiled. A value \u003c= 0 disables profiling. See https://golang.org/pkg/runtime/#SetMutexProfileFraction for details.",
          "type": "integer"
        },
        "save_to_file": {
          "description": "Optional file name to save the CPU profile to. The profiling starts when the Collector starts and is saved to the file when the Collector is terminated.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "telemetry_opentelemetry_collector_contrib_processor_k8sattributesprocessor": {
      "$defs": {
        "exclude_config": {
          "description": "ExcludeConfig represent a list of Pods to exclude",
          "properties": {
            "pods": {
              "items": {
                "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_processor_k8sattributesprocessor/$defs/exclude_pod_config"
              },
              "type": "array"
            }
          },
          "type": "object"
        },
        "exclude_pod_config": {
          "description": "ExcludePodConfig represent a Pod name to ignore",
          "properties": {
            "name": {
              "type": "string"
            }
          },
          "type": "object"
        },
        "extract_config": {
          "description": "ExtractConfig section allows specifying extraction rules to extract data from k8s pod specs.",
          "properties": {
            "annotations": {
              "description": "Annotations allows extracting data from pod annotations and record it as resource attributes. It is a list of FieldExtractConfig type. See FieldExtractConfig documentation for more details.",
              "items": {
                "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_processor_k8sattributesprocessor/$defs/field_extract_config"
              },
              "type": "array"
            },
            "deployment_name_from_replicaset": {
              "description": "DeploymentNameFromReplicaSet allows extracting deployment name from ReplicaSet name by trimming pod template hash. Deprecated: This option now defaults to true and will be removed in future releases.",
              "type": "boolean"
            },
            "labels": {
              "description": "Labels allows extracting data from pod labels and record it as resource attributes. It is a list of FieldExtractConfig type. See FieldExtractConfig documentation for more details.",
              "items": {
                "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_processor_k8sattributesprocessor/$defs/field_extract_config"
              },
              "type": "array"
            },
            "metadata": {
              "description": "Metadata allows to extract pod/namespace/node metadata from a list of metadata fields. The field accepts a list of strings. Metadata fields supported right now are, k8s.pod.name, k8s.pod.uid, k8s.deployment.name, k8s.node.name, k8s.namespace.name, k8s.pod.start_time, k8s.replicaset.name, k8s.replicaset.uid, k8s.daemonset.name, k8s.daemonset.uid, k8s.job.name, k8s.job.uid, k8s.cronjob.name, k8s.cronjob.uid, k8s.statefulset.name, k8s.statefulset.uid, k8s.container.name, container.id, container.image.name, container.image.tag, container.image.repo_digests k8s.cluster.uid Specifying anything other than these values will result in an error. By default, the following fields are extracted and added to spans, metrics and logs as resource attributes: - k8s.pod.name - k8s.pod.uid - k8s.pod.start_time - k8s.namespace.name - k8s.node.name - k8s.deployment.name (if the pod is controlled by a deployment) - k8s.container.name (requires an additional attribute to be set: container.id) - container.image.name (requires one of the following additional attributes to be set: container.id or k8s.container.name) - container.image.tag (requires one of the following additional attributes to be set: container.id or k8s.container.name) — deprecated, use container.image.tags - container.image.tags (requires one of the following additional attributes to be set: container.id or k8s.container.name)",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "otel_annotations": {
              "description": "OtelAnnotations extracts all pod annotations with the prefix \"resource.opentelemetry.io\" as resource attributes E.g. \"resource.opentelemetry.io/foo\" becomes \"foo\"",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "field_extract_config": {
          "description": "FieldExtractConfig allows specifying an extraction rule to extract a resource attribute from pod (or namespace) annotations (or labels).",
          "properties": {
            "from": {
              "description": "From represents the source of the labels/annotations. Allowed values are \"pod\", \"namespace\", \"node\", \"deployment\", \"statefulset\", \"daemonset\", and \"job\". The default is pod.",
              "type": "string"
            },
            "key": {
              "description": "Key represents the annotation (or label) name. This must exactly match an annotation (or label) name.",
              "type": "string"
            },
            "key_regex": {
              "description": "KeyRegex is a regular expression used to extract a Key that matches the regex. Out of Key or KeyRegex, only one option is expected to be configured at a time.",
              "type": "string"
            },
            "tag_name": {
              "description": "TagName represents the name of the resource attribute that will be added to logs, metrics or spans. When not specified, a default tag name will be used of the format: - k8s.pod.annotations.\u003cannotation key\u003e  (or k8s.pod.annotation.\u003cannotation key\u003e when processor.k8sattributes.EmitV1K8sConventions is enabled) - k8s.pod.labels.\u003clabel key\u003e  (or k8s.pod.label.\u003clabel key\u003e when processor.k8sattributes.EmitV1K8sConventions is enabled) For example, if tag_name is not specified and the key is git_sha, then the attribute name will be `k8s.pod.annotations.git_sha` (or `k8s.pod.annotation.git_sha` with the feature gate). When key_regex is present, tag_name supports back reference to both named capturing and positioned capturing. For example, if your pod spec contains the following labels, app.kubernetes.io/component: mysql app.kubernetes.io/version: 5.7.21 and you'd like to add tags for all labels with prefix app.kubernetes.io/ and also trim the prefix, then you can specify the following extraction rules: extract: labels: - tag_name: $$1 key_regex: kubernetes.io/(.*) this will add the `component` and `version` tags to the spans or metrics. When key_regex is present without tag_name, the default tag name format will be used for each matched key. For example: extract: labels: - key_regex: environment\\.(.*) from: pod If labels like \"environment.prod\" and \"environment.dev\" exist, they will be extracted as k8s.pod.labels.environment.prod and k8s.pod.labels.environment.dev respectively.",
              "type": "string"
            }
          },
          "type": "object"
        },
        "field_filter_config": {
          "description": "FieldFilterConfig allows specifying exactly one filter by a field. It can be used to represent a label or generic field filter.",
          "properties": {
            "key": {
              "description": "Key represents the key or name of the field or labels that a filter can apply on.",
              "type": "string"
            },
            "op": {
              "description": "Op represents the filter operation to apply on the given Key: Value pair. The following operations are supported equals, not-equals, exists, does-not-exist.",
              "type": "string"
            },
            "value": {
              "description": "Value represents the value associated with the key that a filter operation specified by the `Op` field applies on.",
              "type": "string"
            }
          },
          "type": "object"
        },
        "filter_config": {
          "description": "FilterConfig section allows specifying filters to filter pods by labels, fields, namespaces, nodes, etc.",
          "properties": {
            "fields": {
              "description": "Fields allows to filter pods by generic k8s fields. Only the following operations are supported: - equals - not-equals Check FieldFilterConfig for more details.",
              "items": {
                "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_processor_k8sattributesprocessor/$defs/field_filter_config"
              },
              "type": "array"
            },
            "labels": {
              "description": "Labels allows to filter pods by generic k8s pod labels. Only the following operations are supported: - equals - not-equals - exists - not-exists Check FieldFilterConfig for more details.",
              "items": {
                "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_processor_k8sattributesprocessor/$defs/field_filter_config"
              },
              "type": "array"
            },
            "namespace": {
              "description": "Namespace filters all pods by the provided namespace. All other pods are ignored.",
              "type": "string"
            },
            "node": {
              "description": "Node represents a k8s node or host. If specified, any pods not running on the specified node will be ignored by the tagger.",
              "type": "string"
            },
            "node_from_env_var": {
              "description": "NodeFromEnv can be used to extract the node name from an environment variable. The value must be the name of the environment variable. This is useful when the node a Otel agent will run on cannot be predicted. In such cases, the Kubernetes downward API can be used to add the node name to each pod as an environment variable. K8s tagger can then read this value and filter pods by it. For example, node name can be passed to each agent with the downward API as follows env: - name: K8S_NODE_NAME valueFrom: fieldRef: fieldPath: spec.nodeName Then the NodeFromEnv field can be set to `K8S_NODE_NAME` to filter all pods by the node that the agent is running on. More on downward API here: https://kubernetes.io/docs/tasks/inject-data-application/environment-variable-expose-pod-information/",
              "type": "string"
            }
          },
          "type": "object"
        },
        "pod_association_config": {
          "description": "PodAssociationConfig contain single rule how to associate Pod metadata with logs, spans and metrics",
          "properties": {
            "sources": {
              "description": "List of pod association sources which should be taken to identify pod",
              "items": {
                "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_processor_k8sattributesprocessor/$defs/pod_association_source_config"
              },
              "type": "array"
            }
          },
          "type": "object"
        },
        "pod_association_source_config": {
          "properties": {
            "from": {
              "description": "From represents the source of the association. Allowed values are \"connection\" and \"resource_attribute\".",
              "type": "string"
            },
            "name": {
              "description": "Name represents extracted key name. e.g. ip, pod_uid, k8s.pod.ip",
              "type": "string"
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/collector_contrib_internal_k8sconfig_api_config"
        }
      ],
      "description": "Config defines configuration for k8s attributes processor.",
      "properties": {
        "exclude": {
          "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_processor_k8sattributesprocessor/$defs/exclude_config"
        },
        "extract": {
          "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_processor_k8sattributesprocessor/$defs/extract_config"
        },
        "filter": {
          "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_processor_k8sattributesprocessor/$defs/filter_config"
        },
        "passthrough": {
          "description": "Passthrough mode only annotates resources with the pod IP and does not try to extract any other metadata. It does not need access to the K8S cluster API. Agent/Collector must receive spans directly from services to be able to correctly detect the pod IPs.",
          "type": "boolean"
        },
        "pod_association": {
          "description": "Association section allows to define rules for tagging spans, metrics, and logs with Pod metadata.",
          "items": {
            "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_processor_k8sattributesprocessor/$defs/pod_association_config"
          },
          "type": "array"
        },
        "pod_delete_grace_period": {
          "anyOf": [
            {
              "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            },
            {
              "type": "integer"
            }
          ],
          "description": "PodDeleteGracePeriod is the duration to wait before deleting a pod from the cache after receiving a delete event."
        },
        "wait_for_metadata": {
          "description": "WaitForMetadata is a flag that determines if the processor should wait k8s metadata to be synced when starting.",
          "type": "boolean"
        },
        "wait_for_metadata_timeout": {
          "anyOf": [
            {
              "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            },
            {
              "type": "integer"
            }
          ],
          "description": "WaitForMetadataTimeout is the maximum time the processor will wait for the k8s metadata to be synced."
        },
        "watch_sync_period": {
          "anyOf": [
            {
              "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            },
            {
              "type": "integer"
            }
          ],
          "description": "WatchSyncPeriod determines the resync period for K8s informers. Reprocessing the informer cache periodically can cause significant memory churn and CPU spikes. Setting this to 0 disables resync."
        }
      },
      "type": "object"
    },
    "telemetry_opentelemetry_collector_contrib_processor_resourcedetectionprocessor": {
      "$defs": {
        "detector_config": {
          "description": "DetectorConfig contains user-specified configurations unique to all individual detectors",
          "properties": {
            "akamai": {
              "$ref": "#/$defs/internal_akamai_config"
            },
            "aks": {
              "$ref": "#/$defs/internal_azure_aks_config"
            },
            "alibaba_ecs": {
              "$ref": "#/$defs/internal_alibaba_ecs_config"
            },
            "azure": {
              "$ref": "#/$defs/internal_azure_config"
            },
            "consul": {
              "$ref": "#/$defs/internal_consul_config"
            },
            "digitalocean": {
              "$ref": "#/$defs/internal_digitalocean_config"
            },
            "docker": {
              "$ref": "#/$defs/internal_docker_config"
            },
            "ec2": {
              "$ref": "#/$defs/internal_aws_ec2_config"
            },
            "ecs": {
              "$ref": "#/$defs/internal_aws_ecs_config"
            },
            "eks": {
              "$ref": "#/$defs/internal_aws_eks_config"
            },
            "elasticbeanstalk": {
              "$ref": "#/$defs/internal_aws_elasticbeanstalk_config"
            },
            "gcp": {
              "$ref": "#/$defs/internal_gcp_config"
            },
            "heroku": {
              "$ref": "#/$defs/internal_heroku_config"
            },
            "hetzner": {
              "$ref": "#/$defs/internal_hetzner_config"
            },
            "ibmcloud_classic": {
              "$ref": "#/$defs/internal_ibmcloud_classic_config"
            },
            "ibmcloud_vpc": {
              "$ref": "#/$defs/internal_ibmcloud_vpc_config"
            },
            "k8s_api": {
              "$ref": "#/$defs/internal_k8sapi_config"
            },
            "k8snode": {
              "$ref": "#/$defs/internal_k8sapi_config"
            },
            "kubeadm": {
              "$ref": "#/$defs/internal_kubeadm_config"
            },
            "lambda": {
              "$ref": "#/$defs/internal_aws_lambda_config"
            },
            "nova": {
              "$ref": "#/$defs/internal_openstack_nova_config"
            },
            "openshift": {
              "$ref": "#/$defs/internal_openshift_config"
            },
            "oraclecloud": {
              "$ref": "#/$defs/internal_oraclecloud_config"
            },
            "scaleway": {
              "$ref": "#/$defs/internal_scaleway_config"
            },
            "system": {
              "$ref": "#/$defs/internal_system_config"
            },
            "tencent_cvm": {
              "$ref": "#/$defs/internal_tencent_cvm_config"
            },
            "upcloud": {
              "$ref": "#/$defs/internal_upcloud_config"
            },
            "vultr": {
              "$ref": "#/$defs/internal_vultr_config"
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_processor_resourcedetectionprocessor/$defs/detector_config"
        },
        {
          "$ref": "#/$defs/io_collector_config_confighttp_client_config"
        }
      ],
      "description": "Config defines configuration for Resource processor.",
      "properties": {
        "detectors": {
          "description": "Detectors is an ordered list of named detectors that should be run to attempt to detect resource information.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "override": {
          "description": "Override indicates whether any existing resource attributes should be overridden or preserved. Defaults to true.",
          "type": "boolean"
        },
        "refresh_interval": {
          "anyOf": [
            {
              "pattern": "^[-+]?((\\d+(\\.\\d+)?|\\.\\d+)(ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            },
            {
              "type": "integer"
            }
          ],
          "description": "If \u003e 0, periodically re-run detection for all configured detectors. When 0 (default), no periodic refresh occurs."
        }
      },
      "type": "object"
    },
    "telemetry_opentelemetry_collector_contrib_processor_resourceprocessor": {
      "description": "Config defines configuration for Resource processor.",
      "properties": {
        "attributes": {
          "description": "AttributesActions specifies the list of actions to be applied on resource attributes. The set of actions are {INSERT, UPDATE, UPSERT, DELETE, HASH, EXTRACT}.",
          "items": {
            "$ref": "#/$defs/internal_coreinternal_attraction_action_key_value"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "telemetry_opentelemetry_collector_contrib_processor_transformprocessor": {
      "description": "Config defines the configuration for the processor.",
      "properties": {
        "error_mode": {
          "$ref": "#/$defs/collector_contrib_pkg_ottl_error_mode"
        },
        "flatten_data": {
          "type": "boolean"
        },
        "log_statements": {
          "items": {
            "$ref": "#/$defs/internal_common_context_statements"
          },
          "type": "array"
        },
        "metric_statements": {
          "items": {
            "$ref": "#/$defs/internal_common_context_statements"
          },
          "type": "array"
        },
        "profile_statements": {
          "items": {
            "$ref": "#/$defs/internal_common_context_statements"
          },
          "type": "array"
        },
        "trace_statements": {
          "items": {
            "$ref": "#/$defs/internal_common_context_statements"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  "$id": "https://raw.githubusercontent.com/coralogix/telemetry-shippers/main/otelcol-ebpf-profiler-schemas/0.155.0-schema.json",
  "properties": {
    "connectors": {
      "additionalProperties": false,
      "patternProperties": {
        "^(otlp|otlpgrpc|otlphttp)(/.*)?$": {
          "anyOf": [
            {
              "type": "object"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "properties": {},
      "type": "object"
    },
    "exporters": {
      "additionalProperties": false,
      "patternProperties": {
        "^(debug|file|nop|otlp|otlp_grpc|otlp_http|otlpgrpc|otlphttp)(/.*)?$": {
          "anyOf": [
            {
              "type": "object"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "properties": {
        "debug": {
          "anyOf": [
            {
              "$ref": "#/$defs/go_opentelemetry_io_collector_exporter_debugexporter"
            },
            {
              "type": "null"
            }
          ]
        },
        "file": {
          "anyOf": [
            {
              "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_exporter_fileexporter"
            },
            {
              "type": "null"
            }
          ]
        },
        "nop": {
          "anyOf": [
            {
              "$ref": "#/$defs/go_opentelemetry_io_collector_exporter_nopexporter"
            },
            {
              "type": "null"
            }
          ]
        },
        "otlp_grpc": {
          "anyOf": [
            {
              "$ref": "#/$defs/go_opentelemetry_io_collector_exporter_otlpexporter"
            },
            {
              "type": "null"
            }
          ]
        },
        "otlp_http": {
          "anyOf": [
            {
              "$ref": "#/$defs/go_opentelemetry_io_collector_exporter_otlphttpexporter"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "extensions": {
      "additionalProperties": false,
      "patternProperties": {
        "^(health_check|opamp|otlp|otlpgrpc|otlphttp|pprof)(/.*)?$": {
          "anyOf": [
            {
              "type": "object"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "properties": {
        "health_check": {
          "anyOf": [
            {
              "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_extension_healthcheckextension"
            },
            {
              "type": "null"
            }
          ]
        },
        "opamp": {
          "anyOf": [
            {
              "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_extension_opampextension"
            },
            {
              "type": "null"
            }
          ]
        },
        "pprof": {
          "anyOf": [
            {
              "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_extension_pprofextension"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "processors": {
      "additionalProperties": false,
      "patternProperties": {
        "^(batch|k8s_attributes|k8sattributes|memory_limiter|otlp|otlpgrpc|otlphttp|resource|resource_detection|transform)(/.*)?$": {
          "anyOf": [
            {
              "type": "object"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "properties": {
        "batch": {
          "anyOf": [
            {
              "$ref": "#/$defs/go_opentelemetry_io_collector_processor_batchprocessor"
            },
            {
              "type": "null"
            }
          ]
        },
        "k8s_attributes": {
          "anyOf": [
            {
              "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_processor_k8sattributesprocessor"
            },
            {
              "type": "null"
            }
          ]
        },
        "memory_limiter": {
          "anyOf": [
            {
              "$ref": "#/$defs/go_opentelemetry_io_collector_processor_memorylimiterprocessor"
            },
            {
              "type": "null"
            }
          ]
        },
        "resource": {
          "anyOf": [
            {
              "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_processor_resourceprocessor"
            },
            {
              "type": "null"
            }
          ]
        },
        "resource_detection": {
          "anyOf": [
            {
              "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_processor_resourcedetectionprocessor"
            },
            {
              "type": "null"
            }
          ]
        },
        "transform": {
          "anyOf": [
            {
              "$ref": "#/$defs/telemetry_opentelemetry_collector_contrib_processor_transformprocessor"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "receivers": {
      "additionalProperties": false,
      "patternProperties": {
        "^(nop|otlp|otlpgrpc|otlphttp|profiling)(/.*)?$": {
          "anyOf": [
            {
              "type": "object"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "properties": {
        "nop": {
          "anyOf": [
            {
              "$ref": "#/$defs/go_opentelemetry_io_collector_receiver_nopreceiver"
            },
            {
              "type": "null"
            }
          ]
        },
        "profiling": {
          "anyOf": [
            {
              "$ref": "#/$defs/opentelemetry_io_ebpf_profiler_receiver_profilingreceiver"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "service": {
      "properties": {
        "extensions": {
          "items": {
            "pattern": "^(health_check|opamp|pprof)(/.*)?$",
            "type": "string"
          },
          "type": "array"
        },
        "pipelines": {
          "additionalProperties": {
            "properties": {
              "exporters": {
                "items": {
                  "pattern": "^(debug|file|nop|otlp|otlp_grpc|otlp_http|otlpgrpc|otlphttp)(/.*)?$",
                  "type": "string"
                },
                "type": "array"
              },
              "processors": {
                "items": {
                  "pattern": "^(batch|k8s_attributes|k8sattributes|memory_limiter|resource|resource_detection|transform)(/.*)?$",
                  "type": "string"
                },
                "type": "array"
              },
              "receivers": {
                "items": {
                  "pattern": "^(nop|otlp|otlpgrpc|otlphttp|profiling)(/.*)?$",
                  "type": "string"
                },
                "type": "array"
              }
            },
            "type": "object"
          },
          "type": "object"
        }
      },
      "type": "object"
    }
  },
  "type": "object"
}